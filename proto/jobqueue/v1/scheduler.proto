syntax = "proto3";

package jobqueue.v1;

import "buf/validate/validate.proto";
import "jobqueue/v1/job.proto";

message CreateJobRequest {
  string task_type = 1 [(buf.validate.field).string = {min_len: 1, max_len: 128}];
  bytes payload = 2 [(buf.validate.field).bytes.max_len = 1048576];
  int32 timeout_seconds = 3 [(buf.validate.field).int32 = {gte: 0, lte: 86400}];
}

message CreateJobResponse {
  Job job = 1;
}

message GetJobRequest {
  string job_id = 1 [(buf.validate.field).string.min_len = 1];
}

message GetJobResponse {
  Job job = 1;
}

message WatchJobRequest {
  string job_id = 1 [(buf.validate.field).string.min_len = 1];
}

message WatchJobResponse {
  JobEvent event = 1;
}

message CancelJobRequest {
  string job_id = 1 [(buf.validate.field).string.min_len = 1];
}

message CancelJobResponse {}

message SubmitBatchRequest {
  CreateJobRequest job = 1 [(buf.validate.field).required = true];
}

message SubmitBatchResponse {
  repeated Job jobs = 1;
  int32 accepted_count = 2;
  int32 rejected_count = 3;
}

service SchedulerService {
  rpc CreateJob(CreateJobRequest) returns (CreateJobResponse);
  rpc GetJob(GetJobRequest) returns (GetJobResponse);
  rpc WatchJob(WatchJobRequest) returns (stream WatchJobResponse);
  rpc CancelJob(CancelJobRequest) returns (CancelJobResponse);
  rpc SubmitBatch(stream SubmitBatchRequest) returns (SubmitBatchResponse);
}